# Copy to .env and `source` it, or export these before `npm start`
export KUBECONFIG=/home/ayush/Desktop/kubeconfig/test.yaml
export OPENCOST_URL=http://localhost:9003
# Optional: kubectl -n prometheus-system port-forward svc/prometheus-server 9091:80
export PROMETHEUS_URL=http://localhost:9091
export PORT=8080
